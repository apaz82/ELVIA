// Servicio para buscar vacantes similares
// Función:
//   getSimilarJobs({ title, location, country }) → GET /api/jobs/similar
// Pasa los parámetros detectados en Página 2 al backend
// El backend los envía a Jooble API y devuelve los resultados formateados
