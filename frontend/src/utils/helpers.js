// Utilidades generales del frontend
// Funciones:
//   formatFileSize(bytes)        → "1.2 MB"
//   validateEmail(email)         → boolean
//   truncateText(text, maxLen)   → texto recortado con "..."
//   getLanguageLabel(code)       → "Español", "English", etc.
